package nn.functional_interface;

public class LamdaExpression {

    // (int p, int q) -> p + q;

    // (p, q) -> p + q;

    //(int p, q) -> (p+q); //Error 

    //(String s) - > s.length()

    //() -> {...}
}
